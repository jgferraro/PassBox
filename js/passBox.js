(function($) {
	$.fn.mask = function(options) {
		
		var settings = $.extend({
			duration: 2000 // Show character for 2 seconds by default
		}, options);
		
		this.each(function() { // loop through all matching selectors 
			
			// plugin code here
			
		});
	}
}(jQuery));